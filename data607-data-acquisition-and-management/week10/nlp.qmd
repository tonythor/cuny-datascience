---
title: "Week 10 NLP"
author: "By Tony Fraser"
date: "November 9, 2023"
format:
  html:
    theme: cosmo
    toc: true
    number_sections: true
---
# Intro to NLP using Taylor Swift Lyrics

```{r}
library(readr)
library(tidytext)
library(wordcloud)
data(stop_words)
library(tidyverse)

song_lyrics <- read_csv("https://raw.githubusercontent.com/tonythor/cuny-datascience/develop/data/taylor_swift_lyrics.csv")  %>%
  unnest_tokens(word, lyric)

# glimpse(lyrics)


```


## A Taylor Swift Word Cloud

```{r}
wc_count = 20
wc_df <- song_lyrics %>% 
  select(word) %>%
  anti_join(stop_words, by = "word") %>%
  count(word) %>%
  filter(n >= wc_count) %>%
  arrange(desc(n))
# -> view here if you like! 
wc_df %>%  with(wordcloud(words = word, freq = n, min.freq = 1, 
                  random.order = FALSE, rot.per = 0.35, 
                  colors = brewer.pal(8, "Dark2")))
```